import styled from 'styled-components'
import Colors from '../../styles/Colors';

export const StyledMembers = styled.main`
  display: block;
  padding: 0;
  margin: 15px auto 0;
  background-color: ${Colors.blockBackground};
`;
