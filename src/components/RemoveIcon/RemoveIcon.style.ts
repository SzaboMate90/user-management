import styled from 'styled-components'
import { CloseCircleOutline } from '@styled-icons/evaicons-outline';

export const iconSize = 20;
export const StyledRemoveIcon = styled(CloseCircleOutline)`
  display: inline;
`;